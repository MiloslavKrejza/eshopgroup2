@model DetailsViewModel
@using Trainee.Core.DAL.Entities

@{
    ViewData["Title"] = "Profil uživatele";
}



<div class="container">
    <h2 class="bottom-padding">@Html.DisplayFor(model => model.Name) @Html.DisplayFor(model => model.Surname)</h2>

    <div class="row row-centered profile">

        <div id="slip">
            <div class="col-lg-5 col-lg-offset-1 profile-left">
                <div class="col-lg-6 profile-info">
                    <p class="profile-item">Email:</p>
                    <p class="profile-item">Telefon:</p>
                    <p class="profile-item">Adresa:</p>
                    <p class="profile-item">Město:</p>
                    <p class="profile-item">PSČ:</p>
                    <p class="profile-item">Země:</p>
                </div>
                <div class="col-lg-6">
                    <p class="second">@Html.DisplayFor(model => model.Email)</p>
                    <p class="second">@Html.DisplayFor(model => model.Phone)</p>
                    <p class="second">@Html.DisplayFor(model => model.Street)</p>
                    <p class="second">@Html.DisplayFor(model => model.City)</p>
                    <p class="second">@Html.DisplayFor(model => model.PostalCode)</p>
                    <p class="second">@Html.DisplayFor(model => model.Country.Name)</p>
                </div>
            </div>


            <div class="col-lg-3 profile-right">
                <img src="~/images/user-2-128.png" alt="profile-photo" class="img-fluid img-profile" /><br />
                <div id="edit-button">
                    <a asp-action="Edit" asp-controller="Account" asp-route-returnurl="@ViewData["ReturnUrl"]">Změnit údaje</a>
                </div>

            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}